import sys

fechas=[]
anos=[]

for line in sys.stdin:
        palabra=line.split(",")
        fechas.append(palabra[2])
fechas.pop(0)
for fecha in fechas:
        separado=fecha.split("-")
        anos.append(separado[0])
for ano in anos:
	print(ano , "1")
